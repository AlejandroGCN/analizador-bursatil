# Nota: Tests eliminados porque pandas_datareader no es compatible con Python 3.12+
# (requiere distutils que fue removido en Python 3.12).
# El adapter funciona correctamente usando yfinance como alternativa.
#
# Para tests funcionales del YahooAdapter, ver tests de integración en tests/integration/
